/* 补丁：统一 t-icon 尺寸与对齐（settings） */
.section-icon {
  font-size: 32rpx;
  color: #1890ff;
  margin-right: 16rpx;
  vertical-align: middle;
}

.setting-arrow {
  font-size: 28rpx;
  color: #bfbfbf;
  vertical-align: middle;
}